% !TeX spellcheck = pt_PT

\chapter{Trabalho Desenvolvido}
\label{chap:4}

Este capítulo apresenta em detalhe a arquitetura, as tecnologias e a implementação dos componentes que constituem o sistema ShopISEL. A solução é composta por múltiplos serviços de backend, duas aplicações cliente (web e móvel), um conjunto de scrapers de dados, um worker de notificações e a infraestrutura de suporte à autenticação, persistência e deployment.

\section{Visão Geral da Arquitetura}
\label{sec:4.1}

O ShopISEL segue uma arquitetura orientada a serviços, em que cada domínio funcional é tratado por um serviço autónomo com a sua própria lógica de negócio e ciclo de deployment, partilhando uma única base de dados PostgreSQL. Na generalidade dos casos, cada serviço gere exclusivamente as suas próprias tabelas; o MatchQueue é uma exceção documentada na Secção~\ref{sec:4.3.4}, por ler e escrever diretamente nas tabelas \texttt{products}, \texttt{categories}, \texttt{prices} e \texttt{favorite\_products}, geridas em primeira instância pelos serviços de domínio correspondentes. Esta separação permite que a maioria dos serviços evolua de forma independente, reduz o acoplamento entre componentes e facilita a escalabilidade horizontal de cada parte do sistema.

A Figura~\ref{fig:arquitetura} apresenta uma visão geral da arquitetura do sistema.

\begin{figure}[H]
\centering
\begin{tikzpicture}[scale=0.68, transform shape,
    node distance=1.0cm and 1.4cm,
    service/.style={rectangle, draw=black, rounded corners=4pt, fill=white,
                    minimum width=2.35cm, minimum height=0.9cm, font=\small, align=center},
    client/.style={rectangle, draw=black!70, rounded corners=4pt, fill=blue!12,
                   minimum width=2.8cm, minimum height=0.9cm, font=\small, align=center},
    db/.style={cylinder, draw=black!70, fill=gray!15, shape border rotate=90,
               minimum width=2.0cm, minimum height=0.8cm, font=\scriptsize, align=center},
    ext/.style={rectangle, draw=black!70, rounded corners=4pt, fill=orange!15,
                minimum width=2.5cm, minimum height=0.9cm, font=\small, align=center},
    gw/.style={rectangle, draw=black, rounded corners=4pt, fill=yellow!20,
               minimum width=3.2cm, minimum height=0.9cm, font=\small\bfseries, align=center},
    arr/.style={-{Stealth[length=6pt]}, thick},
    darr/.style={<->, thick, dashed}
]

% Top row
\node[client] (web) {Web App\\(React + Vite)};
\node[client, right=2.0cm of web] (mobile) {Mobile App\\(React Native)};
\node[ext, left=1.8cm of web, yshift=-0.65cm] (kc) {Keycloak\\(Auth)};

% Gateway
\node[gw, below=1.3cm of $(web)!0.5!(mobile)$] (nginx) {Nginx Gateway};

% Backend services row
\node[service, below left=1.4cm and 2.3cm of nginx] (account) {Account\\Service};
\node[service, right=1.1cm of account] (list) {List\\Service};
\node[service, right=1.1cm of list] (products) {Products\\Service};
\node[service, right=1.1cm of products] (prices) {Prices\\Service};
\node[service, right=1.1cm of prices] (stores) {Stores\\Service};

% Shared database
\node[db, below=1.3cm of $(account)!0.5!(stores)$] (db) {PostgreSQL};

% Client flow
\draw[darr] (kc.south east) -- (nginx.north west);
\draw[arr] (web.south) -- (nginx.north);
\draw[arr] (mobile.south) -- (nginx.north east);

% Gateway to backend services
\draw[arr] (nginx.south) -- ++(0,-0.45) -| (account.north);
\draw[arr] (nginx.south) -- ++(0,-0.45) -| (list.north);
\draw[arr] (nginx.south) -- ++(0,-0.45) -| (products.north);
\draw[arr] (nginx.south) -- ++(0,-0.45) -| (prices.north);
\draw[arr] (nginx.south) -- ++(0,-0.45) -| (stores.north);

% Backend services share a single database (one table per service) and auth
\draw[arr] (account.south) -- ++(0,-0.45) -| (db.north);
\draw[arr] (list.south) -- ++(0,-0.45) -| (db.north);
\draw[arr] (products.south) -- (db.north);
\draw[arr] (prices.south) -- ++(0,-0.45) -| (db.north);
\draw[arr] (stores.south) -- ++(0,-0.45) -| (db.north);
\coordinate (gap-al) at ($(account.east)!0.5!(list.west)$);
\draw[darr] (kc.south) -- ++(0,-2.0) |- (account.west);
\draw[darr] (kc.south) -- ++(0,-1.9) -| (gap-al) -- (list.west);

\end{tikzpicture}
\caption{Visão geral da arquitetura do sistema ShopISEL}
\label{fig:arquitetura}
\end{figure}

Os pedidos das aplicações cliente chegam ao sistema através de um gateway Nginx, que encaminha cada pedido para o serviço responsável com base no caminho do URL. Os serviços de backend partilham uma única base de dados PostgreSQL e, no caso dos serviços protegidos, validam os tokens \ac{JWT} emitidos pelo Keycloak.

A recolha de dados de produtos e preços é feita por scrapers independentes para cada insígnia, que persistem os dados em MongoDB como área de \textit{staging} — um espaço de armazenamento intermédio onde os dados brutos, tal como extraídos de cada website, ficam temporariamente disponíveis antes de serem validados e integrados no modelo de dados definitivo, sem que erros ou inconsistências nesta fase inicial afetem diretamente a base de dados relacional em produção. O MatchQueue processa estes dados, normalizando-os e integrando-os na base de dados relacional. Por fim, o FCM Notification Worker lê as notificações pendentes e envia alertas para os dispositivos dos utilizadores através do Firebase Cloud Messaging. A Figura~\ref{fig:pipeline} apresenta este fluxo de forma separada, para evitar sobreposição com a arquitetura principal.

Por uma questão de foco, a Figura~\ref{fig:pipeline} representa apenas os componentes de execução e o fluxo de dados entre eles. O mecanismo que desencadeia cada etapa — workflows de GitHub Actions que invocam os scrapers e, de seguida, o MatchQueue e o FCM Notification Worker através de eventos \texttt{repository\_dispatch} — não é um componente de runtime e não está representado no diagrama; a orquestração completa é descrita na Secção~\ref{sec:5.4}.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    scale=0.9,
    transform shape,
    node distance=1.0cm and 1.15cm,
    service/.style={rectangle, draw=black, rounded corners=4pt, fill=white,
                    minimum width=2.3cm, minimum height=0.8cm, font=\small, align=center},
    db/.style={cylinder, draw=black!70, fill=gray!15, shape border rotate=90,
               minimum width=1.8cm, minimum height=0.75cm, font=\scriptsize, align=center},
    ext/.style={rectangle, draw=black!70, rounded corners=4pt, fill=orange!15,
                minimum width=2.2cm, minimum height=0.8cm, font=\small, align=center},
    arr/.style={-{Stealth[length=5pt]}, thick},
    ctrl/.style={<->, thick, dashed}
]

\node[service] (sc-cont) {Scraper\\Continente};
\node[service, right=of sc-cont] (orch) {Orchestrator};
\node[service, right=of orch] (sc-pd) {Scraper\\Pingo Doce};

\node[db, below=1.0cm of orch] (mongo) {MongoDB};
\node[service, right=1.35cm of mongo] (mq) {MatchQueue};
\node[service, right=of mq] (fcmw) {FCM\\Worker};
\node[ext, right=1.35cm of fcmw] (fcm) {Firebase\\FCM};
\node[db, below=1.0cm of $(mq)!0.5!(fcmw)$] (pg) {PostgreSQL};

\draw[ctrl] (sc-cont.east) -- (orch.west);
\draw[ctrl] (orch.east) -- (sc-pd.west);
\draw[arr] (sc-cont.south) -- ++(0,-0.12) -| (mongo.north west);
\draw[arr] (sc-pd.south) -- ++(0,-0.12) -| (mongo.north east);
\draw[arr] (orch.south) -- (mq.north);
\draw[arr] (mongo.east) -- (mq.west);
\draw[arr] (mq.south) -- (pg.north);
\draw[arr] (fcmw.south) -- (pg.north east);
\draw[arr] (mq.east) -- (fcmw.west);
\draw[arr] (fcmw.east) -- (fcm.west);

\end{tikzpicture}
\caption{Pipeline de recolha de dados e entrega de notificações}
\label{fig:pipeline}
\end{figure}

\subsection{Modelo de Dados}
\label{sec:4.1.1}

A Figura~\ref{fig:er} apresenta o modelo de dados simplificado do sistema, evidenciando as principais entidades e as relações entre elas. As entidades estão distribuídas pelos diferentes serviços de backend, sendo cada uma gerida pelo serviço responsável pelo respetivo domínio funcional.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    ent/.style={rectangle, draw=black, fill=blue!8, rounded corners=3pt,
                minimum width=3.0cm, minimum height=0.75cm,
                font=\small\bfseries, align=center},
    arr/.style={-{Stealth[length=5pt]}, thick},
    lbl/.style={font=\scriptsize, fill=white, inner sep=1pt}
]

\node[ent] (sl)    at (0,0)     {ShoppingList};
\node[ent] (sli)   at (4.6,0)   {ShoppingListItem};
\node[ent] (prod)  at (9.2,0)   {Product};
\node[ent] (cat)   at (9.2,-2.8){Category};
\node[ent] (store) at (4.6,-2.8){Store};
\node[ent] (price) at (0,-2.8)  {Price};

\draw[arr] (sl.east)     -- node[lbl,above]{1:N} (sli.west);
\draw[arr] (sli.east)    -- node[lbl,above]{N:1} (prod.west);
\draw[arr] (prod.south)  -- node[lbl,right]{N:1} (cat.north);
\draw[arr] (sli.south)   -- node[lbl,right]{N:1} (store.north);
\draw[arr] (price.east)  -- node[lbl,above]{N:1} (store.west);
\draw[arr] (price.north) -- ++(0,0.6) -| node[lbl,above,pos=0.75]{N:1} (prod.south);

\end{tikzpicture}
\caption{Modelo de dados simplificado do sistema ShopISEL. A relação \texttt{parentId} auto-referencial da entidade \textit{Category} está omitida por clareza visual.}
\label{fig:er}
\end{figure}

A Figura~\ref{fig:er} omite, por clareza visual, um conjunto de entidades de suporte relacionadas com contas de utilizador, notificações e o pipeline de recolha de dados. A Tabela~\ref{tab:entidades} completa o modelo, listando todas as entidades persistidas pelos serviços de backend, incluindo as entidades do MatchQueue e os dados brutos armazenados em MongoDB pelos \textit{scrapers}.

\begin{table}[H]
\centering
\caption{Entidades de dados por serviço, complementando o modelo simplificado da Figura~\ref{fig:er}.}
\label{tab:entidades}
\footnotesize
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{2.7cm}|p{8.0cm}|}
\hline
\textbf{Serviço} & \textbf{Entidade} & \textbf{Campos principais} \\
\hline
List Service & ShoppingList & \texttt{Id}, \texttt{OwnerId}, \texttt{Name}, \texttt{CreatedAt}, \texttt{Version} \\
\hline
List Service & ShoppingListItem & \texttt{Id}, \texttt{ListId}, \texttt{ProductId}, \texttt{StoreId}, \texttt{Quantity}, \texttt{Checked} \\
\hline
Products Service & Product & \texttt{Id}, \texttt{Name}, \texttt{Brand}, \texttt{Barcode}, \texttt{Image}, \texttt{CategoryId} \\
\hline
Products Service & Category & \texttt{Id}, \texttt{Name}, \texttt{Image}, \texttt{ParentCategoryId} \\
\hline
Prices Service & Price & \texttt{Id}, \texttt{ProductId}, \texttt{StoreId}, \texttt{PriceText}, \texttt{SaleText}, \texttt{QuantityText}, \texttt{UnitPriceText}, \texttt{SaleDate}, \texttt{UpdatedAt} \\
\hline
Stores Service & Store & \texttt{Id}, \texttt{Name}, \texttt{Brand}, \texttt{Address}, \texttt{City}, \texttt{Latitude}, \texttt{Longitude} \\
\hline
Account Service & Account & \texttt{Id} \\
\hline
Account Service & AccountPushToken & \texttt{Id}, \texttt{AccountId}, \texttt{FcmToken}, \texttt{Platform}, \texttt{DeviceId}, \texttt{IsActive}, \texttt{LastSeenAt}, \texttt{CreatedAt}, \texttt{UpdatedAt} \\
\hline
Account Service & FavoriteProduct & \texttt{AccountId}, \texttt{ProductId} \\
\hline
MatchQueue & NotificationEnqueue & \texttt{Id}, \texttt{AccountId}, \texttt{ProductId}, \texttt{Checked} \\
\hline
MongoDB (\textit{scrapers}) & scrape\_runs & \texttt{\_id}, \texttt{worker\_run\_id}, \texttt{source}, \texttt{status}, \texttt{started\_at}, \texttt{finished\_at}, \texttt{products\_count} \\
\hline
MongoDB (\textit{scrapers}) & scrape\_products & \texttt{\_id}, \texttt{run\_id}, \texttt{source}, \texttt{product\_key}, \texttt{external\_name}, \texttt{price\_text}, \texttt{sale\_text}, \texttt{unit\_price\_text}, \texttt{category\_id}, \texttt{store\_id}, \texttt{scraped\_at} \\
\hline
\end{tabular}
\end{table}

\texttt{NotificationEnqueue} é a única entidade da tabela exclusiva do MatchQueue. As restantes tabelas que o MatchQueue utiliza (\texttt{products}, \texttt{categories}, \texttt{prices}, \texttt{favorite\_products}) já constam desta tabela associadas aos serviços que as gerem em primeira instância; o MatchQueue acede diretamente às mesmas tabelas físicas através de um modelo Entity Framework Core independente, como descrito na Secção~\ref{sec:4.3.4}.

\section{Serviços de Backend}
\label{sec:4.2}

Todos os serviços de backend são desenvolvidos em ASP.NET Core com Minimal \ac{API}s, que reduzem a quantidade de código \textit{boilerplate} — código repetitivo e de estrutura, sem lógica de negócio própria, que seria necessário declarar para cada endpoint (por exemplo, classes de \textit{controller} e respetivos atributos de rota) — face à abordagem tradicional baseada em \textit{controllers}, mantendo a integração completa com o ecossistema .NET. Cada serviço dispõe do seu próprio Dockerfile e pipeline de \ac{CI/CD}, sendo publicado como uma imagem Docker no GitHub Container Registry.

\subsection{List Service}
\label{sec:4.2.1}

O List Service é o serviço central do ShopISEL no que diz respeito à interação do utilizador. Gere a criação, consulta, atualização e eliminação de listas de compras, bem como a coleção de itens associada a cada lista.

O modelo de dados do List Service é composto por duas entidades principais:

\begin{itemize}
    \item \textbf{ShoppingListEntity}: representa uma lista de compras, com um identificador único, o identificador do proprietário (\texttt{OwnerId}, obtido a partir do claim \texttt{sub} do token \ac{JWT}; neste contexto, um \textit{claim} é uma afirmação codificada no token que transporta atributos do utilizador autenticado), o nome da lista, a data de criação e um token de versão (\texttt{Version}) utilizado para controlo de concorrência otimista.
    \item \textbf{ShoppingListItemEntity}: representa um item da lista, com referência ao identificador do produto (\texttt{ProductId}), ao identificador da loja (\texttt{StoreId}), à quantidade desejada e ao estado de marcação (\texttt{Checked}).
\end{itemize}

Toda a persistência é assegurada pelo Entity Framework Core com o provider Npgsql para PostgreSQL, com migrações aplicadas automaticamente no arranque do serviço.

A autenticação é obrigatória em todos os endpoints do List Service. O serviço valida tokens \ac{JWT} emitidos pelo Keycloak, verificando o emissor, a validade temporal e o claim \texttt{azp} (authorized party), que deve corresponder a um dos clientes registados (aplicação web ou móvel). Desta forma, cada lista de compras é sempre associada ao utilizador autenticado, e as operações de leitura e escrita são automaticamente filtradas por \texttt{OwnerId}, garantindo isolamento entre utilizadores.

Os endpoints expostos são:

\begin{itemize}
    \item \texttt{GET /lists} — devolve todas as listas do utilizador autenticado;
    \item \texttt{POST /lists} — cria uma nova lista com a coleção completa de itens fornecida;
    \item \texttt{GET /lists/\{listId\}} — devolve uma lista específica (apenas se pertencer ao utilizador autenticado);
    \item \texttt{PUT /lists/\{listId\}} — atualiza o nome e a coleção completa de itens de uma lista, exigindo o header \texttt{If-Match} com a versão esperada;
    \item \texttt{DELETE /lists/\{listId\}} — elimina uma lista, também condicionada ao header \texttt{If-Match}.
\end{itemize}

As respostas de criação e consulta incluem o campo \texttt{version} no corpo e o header \texttt{ETag}, permitindo às aplicações cliente conhecerem a versão corrente antes de qualquer operação de escrita. Este campo é a base do mecanismo de concorrência otimista adotado no serviço: cada atualização bem-sucedida produz uma nova versão, e qualquer pedido baseado numa versão desatualizada é rejeitado.

O serviço inclui também um endpoint de saúde em \texttt{GET /health}, utilizado pelos mecanismos de monitorização da plataforma de deploy.

\subsubsection{Tratamento de erros e concorrência otimista}
\label{sec:4.2.1.1}

O List Service expõe operações de escrita sobre listas que podem ser invocadas em simultâneo a partir de diferentes clientes — por exemplo, quando o mesmo utilizador edita a mesma lista na aplicação web e na aplicação móvel. Sem mecanismos de proteção, a segunda escrita poderia sobrescrever alterações feitas entretanto pelo primeiro cliente, originando perda silenciosa de dados (\textit{lost updates}).

Para resolver este problema, foi adotada uma estratégia de concorrência otimista, seguindo o princípio descrito por King~\cite{king-optimistic-concurrency}. Cada lista possui um identificador de versão do tipo \texttt{Guid}, gerido pela aplicação e persistido na base de dados como token de concorrência do Entity Framework Core. Sempre que uma lista é criada ou atualizada com sucesso, recebe uma nova versão; nas operações de atualização e eliminação, o serviço compara a versão recebida no header \texttt{If-Match} com a versão atualmente armazenada. Se coincidirem, a operação prossegue; se outro cliente tiver alterado a lista entretanto, o Entity Framework Core deteta o conflito e a API responde com o código HTTP 409 (\textit{Conflict}).

Esta abordagem foi preferida a alternativas como \textit{rowversion} específico de SQL Server, o token nativo \texttt{xmin} do PostgreSQL ou locking pessimista com transações longas. A opção escolhida mantém o comportamento portável entre PostgreSQL, utilizado em produção, e SQLite, utilizado nos testes de integração, sem bloquear recursos de base de dados enquanto o utilizador interage com a interface.

Paralelamente, o tratamento de erros deixou de estar disperso pelos endpoints e passou a ser centralizado num \textit{middleware} de excepções. Este componente traduz falhas de validação, pedidos malformados, conflitos de concorrência, indisponibilidade temporária da base de dados e erros inesperados em respostas HTTP consistentes, com registo em \textit{log} antes de devolver o resultado ao cliente. As respostas de erro de negócio intencionais — como recurso inexistente (404) ou utilizador não autenticado (401) — continuam a ser tratadas ao nível dos endpoints, mantendo separação entre fluxos esperados da aplicação e falhas de infraestrutura.

O impacto na base de dados materializou-se numa migração que adicionou a coluna \texttt{version} à tabela \texttt{lists}, inicializando as linhas existentes com um valor por defeito. Novas listas passam a receber automaticamente um identificador de versão no momento da criação.

\subsection{Products Service}
\label{sec:4.2.2}

O Products Service gere o catálogo de produtos e categorias da plataforma. Não requer autenticação, sendo acessível tanto pelas aplicações cliente como pelos serviços internos.

O modelo de dados contempla duas entidades:

\begin{itemize}
    \item \textbf{ProductEntity}: representa um produto com identificador, nome, código de barras (\texttt{Barcode}), URL de imagem e referência à categoria.
    \item \textbf{CategoryEntity}: representa uma categoria hierárquica, com suporte a subcategorias através de uma relação auto-referencial (\texttt{ParentCategoryId}).
\end{itemize}

A hierarquia de categorias permite organizar os produtos de forma estruturada (e.g., Alimentação → Lacticínios → Leite), facilitando a navegação e filtragem na interface do utilizador.

Os endpoints de produto suportam filtragem por identificadores, por categoria e por nome, bem como uma pesquisa de produtos relacionados com base em favoritos do utilizador, com parâmetros de limite e distância máxima:

\begin{itemize}
    \item \texttt{GET /products?ids=...} — pesquisa por identificadores;
    \item \texttt{GET /products?categoryId=...} — filtragem por categoria;
    \item \texttt{GET /products?name=...} — pesquisa por nome;
    \item \texttt{GET /products/related?favoriteIds=...} — produtos relacionados com os favoritos.
\end{itemize}

O fluxo de correspondência usado no \texttt{/products/qrCode} segue duas fases. Primeiro é tentada uma correspondência exata pelo \texttt{barcode}, procurando no catálogo um produto com o mesmo código. Se isso falhar, o backend normaliza as palavras-chave recebidas, divide-as em tokens úteis e remove termos comuns que não ajudam na identificação. Depois executa uma filtragem inicial sobre o nome e a marca dos produtos para reduzir o conjunto de candidatos e aplica um \textit{ranking} interno com base em cobertura de palavras-chave, número de tokens coincidentes, semelhança textual, distância de Levenshtein~\cite{levenshtein-1966} e um pequeno reforço quando a marca também coincide. Se a correspondência for suficientemente forte, o produto é devolvido como \textit{exact}; caso contrário, são devolvidos produtos \textit{related} para apoiar a escolha do utilizador.

O mesmo critério de distância e semelhança é reutilizado na funcionalidade \texttt{/products/related}, onde o objetivo deixa de ser validar um \texttt{barcode} e passa a ser encontrar produtos próximos dos favoritos do utilizador. Nesse caso, os nomes dos produtos favoritos são normalizados e comparados com candidatos da mesma categoria, usando a distância de Levenshtein para ordenar os resultados por proximidade lexical e devolver uma lista curta de sugestões mais prováveis.

\subsection{Prices Service}
\label{sec:4.2.3}

O Prices Service mantém o preço mais recente de cada produto por loja. A entidade \textbf{PriceEntity} contém o identificador do produto, o identificador da loja, e os valores tal como extraídos pelos \textit{scrapers} em texto livre: \texttt{PriceText} (preço normal), \texttt{SaleText} (preço promocional opcional), \texttt{QuantityText} e \texttt{UnitPriceText} (quantidade e preço unitário, quando disponíveis na página de origem), a data da promoção (\texttt{SaleDate}) e a data da última atualização. Os preços são guardados como texto porque refletem o formato exato apresentado pela insígnia de origem (por exemplo, ``1,99 €''); a conversão para valor numérico, necessária para comparação, ordenação e cálculo de percentagem de desconto, é feita pelas aplicações cliente.

Cada atualização de preço para o mesmo par produto/loja substitui o registo anterior: o MatchQueue, ao processar novos dados de \textit{scraping}, localiza o preço existente para esse produto e loja e atualiza-o \textit{in place}. O sistema não preserva, por isso, os valores anteriores de preço — apenas o preço e a eventual promoção correntes são conhecidos em cada momento. A extensão do modelo para preservar histórico de variações está identificada como trabalho futuro (Capítulo~\ref{chap:7}).

Os endpoints expostos são:

\begin{itemize}
    \item \texttt{GET /prices?productId=\{id\}} — devolve os preços de um produto em todas as lojas disponíveis;
    \item \texttt{GET /prices?productId=\{id\}\&storeId=\{id\}} — devolve o preço de um produto numa loja específica;
    \item \texttt{POST /prices} — regista ou atualiza o preço de um produto numa loja (utilizado internamente pelo pipeline de recolha de dados);
    \item \texttt{GET /health} — endpoint de monitorização.
\end{itemize}

\subsection{Stores Service}
\label{sec:4.2.4}

O Stores Service gere o catálogo de lojas do sistema. Cada loja tem um identificador, nome, insígnia (\texttt{Brand}), morada, cidade e coordenadas geográficas opcionais (\texttt{Latitude}/\texttt{Longitude}), sendo estas últimas usadas para ordenar lojas por proximidade na aplicação cliente.

Na prática, o catálogo distingue dois tipos de registo. Por um lado, existem duas lojas genéricas por insígnia (\texttt{store-1}, \textit{Continente Online}, e \texttt{store-2}, \textit{Pingo Doce Online}), sem coordenadas, que são as únicas lojas a que os \textit{scrapers} de produtos associam preços: todo o histórico de preços do sistema está, atualmente, ancorado nestas duas lojas genéricas, e não em lojas físicas concretas. Por outro lado, a base de dados de produção contém o catálogo completo de lojas físicas das duas insígnias, com morada e coordenadas reais.

Este catálogo de lojas físicas foi obtido com um scraper dedicado (\texttt{all-stores-scraper}), que recolhe morada e coordenadas de todas as lojas do Continente e do Pingo Doce a partir dos respetivos localizadores de lojas, produzindo um total de 908 lojas físicas (406 do Continente e 502 do Pingo Doce). O resultado da recolha foi aplicado à base de dados através do script \ac{SQL} de \textit{upsert} gerado pelo próprio scraper, executado manualmente. Esta operação não está, no entanto, codificada como uma migração do Entity Framework Core nem integrada em nenhuma pipeline de \ac{CI/CD}: é um passo manual e não versionado, pelo que uma nova instância da base de dados criada apenas a partir das migrações do serviço não reproduz o catálogo completo, ficando limitada ao pequeno conjunto de lojas semeado na migração de sementeira. A lacuna não está, portanto, na recolha de dados de lojas — que já cobre a totalidade das duas insígnias —, mas na reprodutibilidade e automatização desse passo de importação.

Os endpoints expostos são:

\begin{itemize}
    \item \texttt{GET /stores} — devolve lojas por identificador ou por nome;
    \item \texttt{POST /stores} — cria uma nova loja;
    \item \texttt{PUT /stores/\{storeId\}} — atualiza uma loja;
    \item \texttt{DELETE /stores/\{storeId\}} — elimina uma loja.
\end{itemize}

\subsection{Account Service}
\label{sec:4.2.5}

O Account Service gere a conta do utilizador autenticado e funciona como ponto de contacto para funcionalidades transversais, como favoritos e tokens de push. O serviço sincroniza a conta a partir do token \ac{JWT} emitido pelo Keycloak e mantém informação operacional associada ao utilizador, sem duplicar a lógica de autenticação.

Os endpoints expostos são:
\begin{itemize}
    \item \texttt{POST /accounts/me/sync} — garante que a conta associada ao token existe na base de dados;
    \item \texttt{GET /accounts/me/favorites} — devolve os produtos favoritos do utilizador autenticado;
    \item \texttt{POST /accounts/me/favorites} — adiciona um produto aos favoritos;
    \item \texttt{DELETE /accounts/me/favorites/\{productId\}} — remove um produto dos favoritos;
    \item \texttt{POST /accounts/me/push-tokens} — regista ou atualiza o token FCM do dispositivo.
\end{itemize}

\section{Pipeline de Recolha de Dados}
\label{sec:4.3}

A recolha automática de dados de produtos e preços é um dos pilares do ShopISEL, procurando manter a informação apresentada ao utilizador atual e coerente com as fontes recolhidas. Neste fluxo, a expressão \textit{worker} refere-se ao modo de execução pontual de componentes que processam uma tarefa e terminam logo após concluí-la, enquanto os serviços backend permanecem expostos como APIs. Como se pode ver na Figura~\ref{fig:pipeline}, o fluxo é iniciado pelo \textit{Scraper Orchestrator}, segue para os scrapers e termina no \textit{MatchQueue} e na entrega de notificações.

\subsection{Scraper Orchestrator}
\label{sec:4.3.1}

O Scraper Orchestrator é um repositório de workflows de GitHub Actions responsável por coordenar a execução dos scrapers de múltiplas insígnias. Cada scraper expõe um workflow reutilizável (\textit{reusable workflow}), que pode ser invocado pelo orchestrator através do mecanismo \texttt{workflow\_call}.

Esta abordagem permite executar os scrapers em paralelo, aguardar a conclusão de todos eles e, de seguida, desencadear o processamento do MatchQueue. A adição de um novo scraper para uma insígnia adicional requer apenas a referência ao seu workflow reutilizável no orchestrator, sem necessidade de alterar os scrapers existentes.

\subsection{Scraper Continente}
\label{sec:4.3.2}

O Scraper Continente é uma aplicação .NET executada como tarefa pontual através de GitHub Actions que utiliza o Playwright para navegar automaticamente no website do Continente e extrair informação de produtos por categoria. O Playwright permite controlar um browser real, sendo adequado para páginas com conteúdo renderizado dinamicamente no cliente, como é o caso do website do Continente~\cite{playwright-docs}.

O scraper percorre as categorias configuradas, efetua scroll para carregar mais produtos (mecanismo de lazy loading), e extrai para cada produto o nome, preço atual, preço anterior, preço por unidade, disponibilidade, \ac{URL} da página e \ac{URL} da imagem. O resultado é exportado para um conjunto de ficheiros JSON no diretório \texttt{output/}: o ficheiro \texttt{continente\_products.json} contém todos os produtos, sendo ainda gerados ficheiros por grupo de categorias (\texttt{continente\_fresh\_products.json}, \texttt{continente\_dairy\_and\_eggs\_products.json}, \texttt{continente\_beverages\_products.json}), bem como ficheiros individuais por categoria no subdiretório \texttt{categories/}.

A estrutura do scraper divide as responsabilidades em módulos distintos:
\begin{itemize}
    \item \texttt{Scrapers/}: contém a lógica de navegação e extração por categoria;
    \item \texttt{Parsing/}: realiza o parsing do HTML e a normalização de texto (preços, unidades);
    \item \texttt{Models/}: define as estruturas de dados para fontes de categoria e produtos extraídos;
    \item \texttt{Config/}: configuração das fontes de scraping (categorias e URLs de entrada);
    \item \texttt{Matching/}: lógica de correspondência de produtos entre fontes;
    \item \texttt{Sync/}: sincronização dos dados extraídos com o MongoDB.
\end{itemize}

\subsection{Scraper Pingo Doce}
\label{sec:4.3.3}

O Scraper Pingo Doce segue a mesma abordagem baseada em Playwright, adaptada à estrutura do website do Pingo Doce. Adicionalmente, suporta dois modos de execução:

\begin{itemize}
    \item \textbf{Modo normal}: efetua o scraping ao vivo e persiste os resultados em MongoDB;
    \item \textbf{Modo \texttt{--from-json}}: processa um ficheiro JSON previamente gerado, evitando pedidos desnecessários ao website.
\end{itemize}

Os dados são persistidos em MongoDB~\cite{mongodb-docs} em duas coleções:
\begin{itemize}
    \item \texttt{scrape\_runs}: um documento por execução, identificado por um \texttt{run\_id} único;
    \item \texttt{scrape\_products}: um documento por produto por execução, com um identificador estável baseado no URL externo ou numa combinação de categoria e nome.
\end{itemize}

Esta estratégia de identificação estável permite ao MatchQueue detetar alterações de preço entre execuções consecutivas, comparando os valores pelo mesmo identificador de produto.

O scraper inclui um workflow de GitHub Actions que o executa de forma agendada (cron), lendo as credenciais de acesso ao MongoDB a partir de segredos do repositório.

\subsection{MatchQueue}
\label{sec:4.3.4}

O MatchQueue é um serviço ASP.NET Core que expõe uma API própria para desencadear o processamento de correspondência, mas essa via não é utilizada no fluxo operacional do projeto. Na prática, o componente é executado apenas pelo GitHub Actions em modo \textit{worker} após a conclusão dos scrapers; nesse modo, processa o evento \texttt{repository\_dispatch}, executa a normalização e termina a execução.
Este componente faz a ponte entre os dados brutos recolhidos pelos scrapers, guardados em MongoDB, e os dados já preparados para uso no resto do sistema. Para isso, o MatchQueue usa diretamente as mesmas tabelas do Products Service, do Prices Service e do Account Service, em vez de guardar cópias separadas. Na prática, isto significa que o MatchQueue lê e escreve nas tabelas \texttt{products}, \texttt{categories}, \texttt{prices} e \texttt{favorite\_products} para registar o resultado do processamento.

Esta solução funciona, mas exige cuidado: se um serviço alterar a estrutura de uma tabela, os outros têm de acompanhar essa mudança manualmente. Um exemplo é a coluna \texttt{canonical\_key} da tabela \texttt{products}, que foi adicionada pelo MatchQueue e não é usada pelo Products Service. Outro exemplo é a tabela \texttt{categories}: o MatchQueue apenas usa o campo \texttt{Id}, enquanto o Products Service é responsável por \texttt{name}, \texttt{image} e \texttt{parent\_category\_id}. Por isso, cada serviço trabalha apenas com uma parte da mesma tabela.

Além destas entidades partilhadas, o MatchQueue tem uma entidade própria, sem equivalente noutro serviço:
\begin{itemize}
    \item \textbf{NotificationEnqueueEntity}: fila de notificações pendentes, com referência ao utilizador, ao produto e ao estado de processamento (\texttt{Checked}), persistida na tabela \texttt{notification\_enque}, usada para determinar quais os utilizadores a notificar quando um produto favorito desce de preço.
\end{itemize}
O MatchQueue é orquestrado por um workflow de GitHub Actions que corre após a conclusão dos scrapers, permitindo encadear a recolha, a normalização e o processamento de notificações como uma etapa temporária da pipeline. Nesta fase, o GitHub Actions funciona como camada de orquestração e não como o tipo de aplicação em si.

\section{FCM Notification Worker}
\label{sec:4.4}

O FCM Notification Worker é uma aplicação .NET de execução pontual (\textit{run-to-completion}): ao contrário dos serviços de backend, não possui imagem Docker nem deployment persistente, sendo compilada e executada diretamente num \textit{runner} do GitHub Actions sempre que recebe o evento \texttt{repository\_dispatch} disponibilizado automaticamente pelo workflow do MatchQueue ao concluir o seu processamento (podendo também ser executada manualmente). O Firebase Cloud Messaging (\ac{FCM})~\cite{firebase-fcm} é um serviço de entrega de mensagens push mantido pela Google, que permite enviar notificações para dispositivos Android e iOS de forma fiável e escalável. O seu funcionamento segue os seguintes passos:

\begin{enumerate}
    \item Inicializa o cliente Firebase com as credenciais, guardadas como segredo e codificadas em Base64 apenas para permitir a sua injeção como variável de ambiente de texto simples (o Base64 não constitui, por si só, um mecanismo de proteção);
    \item Estabelece ligação à base de dados PostgreSQL do MatchQueue;
    \item Consulta a tabela \texttt{notification\_enque} para obter notificações pendentes não processadas (\texttt{checked = false}), cruzando com a tabela \texttt{account\_push\_tokens} para obter os tokens FCM ativos dos utilizadores;
    \item Constrói e envia as mensagens push em lotes de 500 (limite imposto pelo Firebase);
    \item Marca as notificações processadas como \texttt{checked = true}.
\end{enumerate}

As notificações enviadas informam o utilizador de que o preço de um produto favorito desceu, incluindo nos dados da mensagem o identificador do produto e da notificação, permitindo à aplicação móvel navegar diretamente para o produto relevante.

\section{Aplicação Web}
\label{sec:4.5}

A aplicação web do ShopISEL é desenvolvida em React com TypeScript e Vite, proporcionando uma interface moderna e responsiva para preparação e análise de compras num ambiente de desktop.

A estrutura da aplicação segue uma organização por responsabilidade:
\begin{itemize}
    \item \texttt{src/api/}: camada de comunicação com os serviços de backend, abstraindo os detalhes de cada pedido HTTP;
    \item \texttt{src/auth/}: integração com o Keycloak para autenticação, gestão do ciclo de vida do token e redirecionamentos;
    \item \texttt{src/app/}: componentes de página e lógica de navegação;
    \item \texttt{src/styles/}: estilos globais e configuração de temas.
\end{itemize}

A interface recorre a bibliotecas de componentes como Material UI, Radix UI e Tailwind CSS, que permitem construir ecrãs consistentes e acessíveis com menor esforço de implementação. O TypeScript garante a verificação estática dos tipos trocados com as APIs, reduzindo erros em tempo de execução.

A aplicação web é servida em produção através de um servidor Nginx, sendo a imagem Docker publicada automaticamente pelo pipeline de \ac{CI/CD}.

\subsection{Integração com Concorrência Otimista}
\label{sec:4.5.1}

A aplicação web inclui suporte para concorrência otimista através da integração com o token de versão exposto pelo contrato do \textit{List Service}. A camada de comunicação com a API modela o campo \texttt{version} em cada lista, incluindo-o de forma automática no cabeçalho (\textit{header}) \texttt{If-Match} em todos os pedidos de atualização e eliminação.

Os ecrãs de gestão de listas — nomeadamente a página de detalhe, o ecrã inicial, a listagem geral e o painel de adição de produtos — asseguram a propagação da versão corrente em cada operação de escrita. Sempre que ocorrem ações como a alteração do nome, marcação de itens, ou a adição e remoção de produtos, a aplicação submete a versão correspondente ao momento do carregamento inicial dos dados. 

Caso o \textit{backend} detete que a lista foi modificada concorrencialmente por outro cliente, o pedido é rejeitado com o código de estado 409. Nestes cenários, a interface apresenta uma mensagem informativa que indica que a lista foi alterada noutro dispositivo, instruindo o utilizador a recarregar os dados antes de redefinir a operação. Este mecanismo garante a integridade dos dados e previne a perda silenciosa de informação em cenários de acesso concorrente.
\subsection{Arquitetura white-label dinâmica}
\label{sec:4.5.2}

Para permitir a publicação da aplicação web sob diferentes identidades visuais sem manter ramificações de código separadas, foi implementado um sistema de personalização em tempo de \textit{build}. O objetivo é alterar nome, slogan, logótipo e esquema de cores da marca apenas através de parâmetros injetados no processo de construção e \textit{deployment}, mantendo o código-fonte partilhado focado na lógica funcional da aplicação.

A configuração de marca concentra-se num módulo central que lê variáveis de ambiente prefixadas com \texttt{VITE\_} e aplica valores por defeito correspondentes à identidade Shopisel quando nenhuma personalização é fornecida. Para além dos elementos textuais e do logótipo, o módulo calcula automaticamente tons derivados — versões mais claras, mais escuras e com transparência — a partir das cores primária e secundária indicadas, evitando que cada variante exija a definição manual de dezenas de valores cromáticos.

No arranque da aplicação, as cores resolvidas são injetadas como propriedades CSS personalizadas no documento, o que permite que as classes utilitárias do Tailwind e os componentes existentes adotem imediatamente a nova paleta. Elementos como a barra lateral, o ecrã de \textit{splash}, o \textit{onboarding} e os controlos de navegação passam a refletir a marca configurada sem alterações estruturais ao código.

O fluxo completo de personalização percorre o pipeline de \ac{CI/CD}: o workflow de publicação Docker suporta execução manual com inputs opcionais para os parâmetros de marca; estes valores são passados como argumentos de construção da imagem, convertidos em variáveis de ambiente durante o \textit{build} do Vite e incorporados de forma estática no \textit{bundle} final. Quando o workflow corre automaticamente após integração no branch principal, os inputs assumem valores vazios e a aplicação mantém a identidade por defeito. Desta forma, a ativação de uma nova variante white label reduz-se a executar o workflow de \textit{deploy} com os parâmetros desejados, sem necessidade de editar o repositório.

\section{Aplicação Móvel}
\label{sec:4.6}

A aplicação móvel é desenvolvida em React Native com o framework Expo, permitindo uma única base de código para Android e iOS. O Expo Router organiza a navegação por ficheiros e separa o fluxo principal em cinco separadores: \textit{Home}, \textit{Lists}, \textit{Scan}, \textit{Prices} e \textit{Profile}. Para além destes separadores, existem ainda ecrãs dedicados a \textit{Onboarding}, \textit{Auth} e \textit{Alerts}, que são apresentados no \textit{stack} principal da aplicação.

\subsection{Leitura de código de barras}
\label{sec:4.6.1}

O ecrã de leitura de código de barras é um dos pontos mais práticos da experiência móvel. A aplicação usa a câmara do dispositivo para capturar o código, extrai o identificador e complementa a informação do produto com o serviço público Open Food Facts quando o catálogo local não é suficiente. De seguida, envia ao endpoint \texttt{/products/qrCode} um corpo \ac{JSON} com o \texttt{barcode} lido e as \texttt{keywords} obtidas a partir dos dados complementares, permitindo ao backend tentar primeiro uma correspondência exata e, se necessário, devolver produtos relacionados~\cite{gs1-ean-upc,openfoodfacts-api}.

O fluxo implementado distingue três resultados distintos no ecrã de scan: produto encontrado, apenas produtos relacionados e nenhum resultado. Quando existe um produto exato, a aplicação apresenta a ação de adicionar à lista, abre uma modal para escolher a lista de destino e guarda o produto lido localmente para que possa ser reutilizado no separador \textit{Prices} quando não exista \texttt{productId} na rota. Quando surgem apenas produtos relacionados, estes são apresentados como itens clicáveis, sem botões adicionais desnecessários. Se não for encontrado qualquer resultado, o utilizador recebe uma mensagem de falha e pode tentar novamente ou pesquisar manualmente pelo nome do produto.

Este fluxo pede autorização explícita para aceder à câmara quando o ecrã de scan é aberto e a permissão ainda não foi concedida. A interface apresenta um estado próprio de permissões e só ativa a leitura depois de o utilizador autorizar o acesso, garantindo que o scan funciona de forma controlada e transparente.

\subsection{Localização e preços próximos}
\label{sec:4.6.2}

A componente de preços próximos utiliza o módulo de localização do Expo para obter a posição do utilizador. Como o preço devolvido pelo Prices Service está associado a uma das duas lojas genéricas por insígnia (Secção~\ref{sec:4.2.4}), a aplicação usa a insígnia dessa loja para procurar, entre as lojas físicas com coordenadas da mesma insígnia, as três mais próximas do utilizador, e apresenta o preço sob o nome e morada dessas lojas. Ou seja, a proximidade determina que loja física é mostrada ao utilizador, mas não afeta o valor do preço apresentado, que é o mesmo em todas as lojas da insígnia. Quando não existem coordenadas disponíveis — do utilizador ou de lojas físicas dessa insígnia — a aplicação apresenta a loja genérica, sem indicação de distância. O objetivo não foi criar um sistema de georreferenciação complexo no servidor, mas sim aproveitar a localização do dispositivo para dar contexto à consulta de preços e destacar lojas próximas no momento da compra.

A obtenção da localização também depende de autorização do utilizador: o \textit{hook} de localização pede permissão em \textit{foreground} antes de obter e acompanhar a posição atual. Quando a permissão é recusada, a aplicação mostra essa limitação de forma explícita e continua a funcionar, apenas sem ordenar os preços por proximidade.

A organização do código fonte por domínio funcional facilita a manutenção e a extensão da aplicação:
\begin{itemize}
    \item \texttt{src/api/}: chamadas aos serviços de backend;
    \item \texttt{src/auth/}: gestão do token de autenticação com armazenamento seguro;
    \item \texttt{src/push/}: registo do token FCM e integração com notificações push;
    \item \texttt{src/location/}: obtenção da localização do utilizador;
    \item \texttt{src/favorites/}: gestão dos produtos favoritos do utilizador;
    \item \texttt{src/i18n/}: suporte a múltiplos idiomas;
    \item \texttt{src/theme/}: sistema de temas e estilos globais.
\end{itemize}

O NativeWind é utilizado para estilização, aplicando as utilidades do Tailwind CSS ao contexto React Native~\cite{nativewind-docs}, garantindo consistência visual com a aplicação web. O armazenamento seguro dos tokens de autenticação utiliza o Expo Secure Store, que persiste dados de forma cifrada no dispositivo.

\section{Autenticação e Autorização}
\label{sec:4.7}

A autenticação e autorização no ShopISEL é centralizada no Keycloak, uma plataforma de gestão de identidade e acesso que implementa os protocolos \ac{OIDC} e OAuth 2.0.

O fluxo de autenticação segue o padrão Authorization Code Flow com PKCE (Proof Key for Code Exchange), adequado tanto para aplicações web como para aplicações móveis. O utilizador é redirecionado para o servidor Keycloak para introduzir as suas credenciais; após autenticação bem-sucedida, é emitido um token de acesso \ac{JWT} que é enviado em cada pedido aos serviços protegidos. A Figura~\ref{fig:keycloak-flow} ilustra este fluxo.

Os serviços que requerem autenticação (List Service e Account Service) validam o token da seguinte forma:
\begin{enumerate}
    \item Verificam que o emissor (\texttt{iss}) corresponde ao realm Keycloak configurado;
    \item Verificam que o token não expirou (\texttt{exp});
    \item Verificam que a chave de assinatura corresponde à chave pública do Keycloak (obtida via OIDC Discovery);
    \item Verificam que o claim \texttt{azp} (authorized party) corresponde a um dos clientes autorizados (web, mobile).
\end{enumerate}

Esta abordagem evita que cada serviço implemente autenticação própria, centraliza a gestão de utilizadores no Keycloak e permite que novos serviços se integrem facilmente no ecossistema de autenticação existente.

No registo de uma nova conta, o Keycloak exige a confirmação do endereço de email antes de permitir o primeiro login, através da \textit{required action} nativa \textit{Verify Email}: após a criação da conta, é enviado automaticamente um email de confirmação com uma ligação que valida o endereço e desbloqueia o acesso. O envio deste email é feito através do Brevo, configurado como servidor \ac{SMTP} do realm do Keycloak, sem necessidade de código adicional nos serviços do ShopISEL.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    font=\scriptsize,
    line/.style={draw=black, thick},
    dashedline/.style={draw=black, thick, dashed},
    box/.style={rectangle, draw=black!70, rounded corners=2pt, minimum width=2.9cm, minimum height=0.9cm, align=center},
    arrow/.style={-{Stealth[length=6pt]}, thick}
]

\node[box] (userTop) at (0,4.1) {Utilizador\\(Browser)};
\node[box] (appTop) at (4.2,4.1) {App (Client)};
\node[box] (kcTop) at (8.6,4.1) {Keycloak};
\node[box] (apiTop) at (13.0,4.1) {Backend/API};

\node[box] (userBot) at (0,-4.0) {Utilizador\\(Browser)};
\node[box] (appBot) at (4.2,-4.0) {App (Client)};
\node[box] (kcBot) at (8.6,-4.0) {Keycloak};
\node[box] (apiBot) at (13.0,-4.0) {Backend/API};

\draw[line] (0,3.6) -- (0,-3.6);
\draw[line] (4.2,3.6) -- (4.2,-3.6);
\draw[line] (8.6,3.6) -- (8.6,-3.6);
\draw[line] (13.0,3.6) -- (13.0,-3.6);

\draw[arrow] (0,3.1) -- node[midway, above, font=\scriptsize, fill=white, inner sep=1pt] {Abre app} (4.2,3.1);
\draw[arrow] (4.2,2.35) -- node[midway, above, font=\scriptsize, fill=white, inner sep=1pt, align=center] {Redirect para\\\texttt{/authorize}} (8.6,2.35);
\draw[arrow] (8.6,1.6) -- node[midway, above, font=\scriptsize, fill=white, inner sep=1pt] {Mostra login / MFA} (0,1.6);
\draw[arrow] (0,0.85) -- node[midway, above, font=\scriptsize, fill=white, inner sep=1pt] {Credenciais} (8.6,0.85);
\draw[dashedline] (8.6,0.1) -- node[midway, above, font=\scriptsize, fill=white, inner sep=1pt, align=center] {Redirect com\\authorization code} (4.2,0.1);
\draw[arrow] (4.2,-0.65) -- node[midway, above, font=\scriptsize, fill=white, inner sep=1pt] {Troca code por tokens} (8.6,-0.65);
\draw[dashedline] (8.6,-1.4) -- node[midway, above, font=\scriptsize, fill=white, inner sep=1pt, align=center] {Access / ID /\\Refresh token} (4.2,-1.4);
\draw[arrow] (4.2,-2.15) -- node[midway, above, font=\scriptsize, fill=white, inner sep=1pt, align=center] {Chama API com\\Access Token (Bearer)} (13.0,-2.15);
\draw[arrow] (13.0,-2.9) -- node[midway, above, font=\scriptsize, fill=white, inner sep=1pt, align=center] {Valida token\\(JWKS/introspection)} (8.6,-2.9);
\draw[dashedline] (8.6,-3.3) -- node[midway, below, font=\scriptsize, fill=white, inner sep=1pt] {Resposta} (4.2,-3.3);

\end{tikzpicture}
\caption{Fluxo de autenticação com Keycloak}
\label{fig:keycloak-flow}
\end{figure}

\section{Gateway e Infraestrutura de Deployment}
\label{sec:4.8}

O acesso às APIs dos serviços de backend é feito através de um gateway Nginx, que atua como ponto de entrada único para as aplicações cliente. O Nginx encaminha os pedidos para o serviço correto com base no caminho do URL:

\begin{itemize}
    \item \texttt{/api/lists/...} → List Service;
    \item \texttt{/api/products/...} → Products Service;
    \item \texttt{/api/categories/...} → Products Service (categorias);
    \item \texttt{/api/prices/...} → Prices Service;
    \item \texttt{/api/stores/...} → Stores Service;
    \item \texttt{/api/accounts/...} → Account Service;
    \item \texttt{/} → Aplicação Web (frontend).
\end{itemize}

Esta arquitetura de gateway simplifica a configuração das aplicações cliente, que comunicam com um único endpoint base, e permite adicionar funcionalidades transversais como rate limiting, logging centralizado ou cache HTTP sem modificar os serviços individuais.

Todo o sistema é containerizado com Docker. Cada serviço dispõe de um Dockerfile que produz uma imagem mínima e otimizada para produção. Os serviços são implantados no Fly.io, tirando partido dos mecanismos de deployment e operação disponibilizados pela plataforma.

A configuração de deployment está declarada nos ficheiros de infraestrutura e nos pipelines de \ac{CI/CD}, que publicam as imagens no GitHub Container Registry e automatizam o processo de entrega.
